# Core framework
langchain>=1.3,<2.0
langchain-anthropic>=1.6,<2.0

# Claude API SDK (used by langchain-anthropic).
# Capped below 1.0 because langchain-anthropic 1.6.x requires anthropic<1.0.0.
# Raise this once langchain-anthropic supports the anthropic 1.x SDK.
anthropic>=0.125,<1.0

# Web UI
streamlit>=1.62,<2.0

# Configuration
python-dotenv>=1.2,<2.0

# Testing
pytest>=9.1,<10.0
